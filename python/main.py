import pygame, picking

#takes in keyboard intakes (0-6)
def keyboardIn():
	pygame.init()
	while True:
		pressed = pygame.key.get_pressed()
		#if statements for checking which key is pressed
		

if __name__ == 'main':
	keyboardIn()